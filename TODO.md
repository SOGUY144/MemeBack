# สิ่งที่จะทำต่อ

จัดลำดับตามผลกระทบต่อการเอาไปใช้สอนจริง ไม่ใช่ตามความยาก

---

## 0. ต้องแก้ก่อนเอาไปใช้สอนจริง

### 0.2 ไม่มี API key = เว็บไม่ได้ทำงานหลักของมัน
ดูข้อ 2 ด้านล่าง — ทุกคำตอบได้ฉากสำรองเหมือนกันหมด ไม่มีมีมส่วนตัวจริง
**เจ้าของเครื่องต้องใส่คีย์ใน `.env` เอง**

---

## 1. แก้แล้ว (14 ส.ค. 2569)

### 1.1 QR ชี้ไปที่ localhost — แก้แล้ว
เพิ่ม `src/app/api/join-url/route.ts` หา IPv4 วงในจาก `os.networkInterfaces()`
เรียงลำดับความน่าใช้ `192.168/16` → `10/8` → `172.16/12` และตัด `169.254/16` (DHCP ล้มเหลว) ทิ้ง
จอฉายเรียก route นี้ก่อนสร้าง QR ถ้าเรียกไม่ได้ค่อยถอยไปใช้ `window.location.origin`
และโชว์ URL เป็นตัวหนังสือใต้ QR ให้คนที่สแกนไม่ติดพิมพ์เอง

ตั้ง `PUBLIC_ORIGIN` ทับได้ ถ้าเสิร์ฟผ่าน tunnel หรือ HTTPS จริง

**ยังจริงอยู่:** `http://192.168.x.x:3000` ไม่ใช่ secure context → `VideoEncoder` ใช้ไม่ได้
ทุกเครื่องจะตกไปใช้ gif.js (ไฟล์ ~582KB แทน ~55KB) ถ้าจะให้ดีต้องเสิร์ฟผ่าน HTTPS แล้วตั้ง `PUBLIC_ORIGIN`

### 1.2 นักเรียนมาสายเข้าห้องไม่ได้ — แก้แล้ว
ตัดเงื่อนไขเฟสใน `room:join` ออก เข้าได้ทุกเฟสแล้ว
คนที่เข้ามากลางคันไม่มีคำตอบของตัวเองก็แค่ดู แล้วร่วมทายตั้งแต่รอบถัดไป
คนที่มือถือดับหรือล้าง localStorage ก็กลับเข้ามาได้ (เป็นผู้เล่นใหม่ คะแนนเดิมไม่ตามมา)

### 1.3 แถบความคืบหน้าบนจอฉายเป็นของปลอม — แก้แล้ว
เพิ่ม `analyzedCount` ใน `RoomState` นับจาก stage ที่อยู่ในหน่วยความจำก่อน ถ้าไม่มีค่อยดู `verdict` ในฐานข้อมูล
(ดู verdict อย่างเดียวไม่พอ เพราะคำตอบที่ส่งใหม่จะคง verdict เดิมไว้จนผลใหม่มาถึง)
จอฉายคิด `analyzedCount / answeredCount` และเปลี่ยนข้อความใต้แถบเป็น *อ่านคำตอบแล้ว n / total*

### 1.4 คำตอบ `off_topic` เป็นทางตัน — แก้แล้ว
เลือกทางแรกตามที่เขียนไว้ ทั้งฝั่งเซิร์ฟเวอร์และหน้าจอ

- เซิร์ฟเวอร์: `answer:submit` เปิดให้เฉพาะคนที่ verdict เดิมเป็น `off_topic` ส่งซ้ำได้ในเฟส `GENERATING` และ `PERSONAL_REVEAL` ด้วย คำตอบอื่นยังปิดตามเดิม
- นักเรียน: การ์ด *ลองเขียนใหม่* มี textarea กับปุ่มส่งจริง โผล่ทั้งสองเฟส และขึ้น *กำลังอ่านคำตอบ* ระหว่างวิเคราะห์รอบใหม่

**ข้อจำกัดที่ตั้งใจ:** คำตอบที่ส่งซ้ำหลังเข้า `PERSONAL_REVEAL` แล้วจะไม่ถูกเลือกขึ้นจออัตโนมัติ
เพราะการรัน `applyAutoPromotions()` ซ้ำจะทับสิ่งที่ครูเลือกเองไว้แล้ว ครูกดขึ้นจอเองได้จากแผงควบคุม

### 1.5 เข้าห้องกลางเฟส `REVEAL`/`SCOREBOARD` แล้วเจอจอเปล่า — แก้แล้ว
ผลข้างเคียงของ 1.2 เดิม `catchUpOnGuessRound()` ตามให้เฉพาะเฟส `CLASS_GUESS`
พอเปิดให้เข้าได้ทุกเฟส คนที่เข้ามาตอนเฉลยหรือตอนดูคะแนนจะไม่ได้รับ `reveal:answer` และ `scoreboard`
ซึ่งยิงตอนเปลี่ยนเฟสครั้งเดียว

เปลี่ยนเป็น `catchUpOnPhase()` ตามให้ครบทั้ง `CLASS_GUESS` `REVEAL` `SCOREBOARD`
แยก `buildRevealPayload()` กับ `scoreboardRows()` ออกมาเป็นฟังก์ชันไม่มี side effect
เพื่อไม่ให้คนที่เข้ามาทีหลังไปกระตุ้นการจ่ายโบนัสเจ้าของมีมซ้ำ
และเรียกให้ครูด้วย — เดิมครูรีเฟรชตอน `REVEAL` แล้วประเด็นสอนกับตัวนับ *มีมถัดไป* หายทั้งรอบ

**ลำดับการยิงสำคัญ:** ต้องส่ง `guess:card` ก่อน `reveal:answer` เพราะฝั่งไคลเอนต์ล้างสถานะเฉลยทิ้งเมื่อได้การ์ดใหม่

### 1.6 ครูกด "ขึ้นจอ" ระหว่างรอบทาย → ทั้งห้องส่งคำทายไม่ได้ — แก้แล้ว
`teacher:promote` สั่ง `rt.guessOrder = []` เสมอไม่ว่าอยู่เฟสไหน ทำให้ `guess:submit` เทียบ
`rt.guessOrder[rt.guessIndex] !== p.answerId` แล้วปฏิเสธทุกคนด้วยข้อความ *มีมนี้ไม่ได้อยู่บนจอแล้ว*
ทั้งที่มีมยังอยู่บนจอ ปุ่มขึ้นจอโชว์อยู่ตลอดทุกเฟส เลยกดโดนได้ง่ายมาก

ตอนนี้ **ลำดับมีมล็อกทันทีที่เข้า `CLASS_GUESS`** เซิร์ฟเวอร์ปฏิเสธการเปลี่ยนในเฟส `CLASS_GUESS` และ `REVEAL`
เพิ่ม ack ให้ `teacher:promote` เพื่อบอกเหตุผลกลับไป และปิดปุ่มในแผงครูให้ตรงกับพฤติกรรมจริง
(ปิดปุ่มอย่างเดียวไม่พอ — บังคับที่เซิร์ฟเวอร์ด้วยเสมอ)

### 1.7 ชื่อเล่นซ้ำกันได้ — แก้แล้ว
`uniqueNickname()` เติมเลขต่อท้ายให้คนที่สอง (`ปาล์ม` → `ปาล์ม 2`) แทนที่จะไล่กลับ
เพราะมือถือกลางห้องเรียนไม่ใช่ที่ที่ควรมาเถียงเรื่องชื่อ ตัดความยาวฐานให้ผลรวมไม่เกิน 20 ตัวอักษร

### 1.8 คำตอบที่วิเคราะห์ล้มเหลวเป็นทางตัน — แก้แล้ว
เพิ่ม event `teacher:reanalyze` (เฉพาะครู ผูก scope ผ่าน `question.room.code` กันสั่งข้ามห้อง)
และปุ่ม *วิเคราะห์ใหม่* จะโผล่ในแผงครูเฉพาะแถวที่ `stage === 'failed'`

### 1.9 ไฟล์มีมไม่เคยถูกลบ — แก้แล้ว
`cleanupOldMemes()` ลบไฟล์ของคำถามรอบก่อนตอนครูตั้งคำถามใหม่ พร้อมเคลียร์ `memeUrl` ในฐานข้อมูล
ปลอดภัยเพราะ `currentQuestion()` เรียงตาม `createdAt desc` — คำถามเก่าไม่มีทางถูกเรียกดูอีก
ทำแบบ best-effort ลบไม่สำเร็จต้องไม่ทำให้คาบเรียนสะดุด

---

## 2. ยังไม่ได้พิสูจน์ ต้องมี API key ก่อน

ตอนนี้ยังไม่มี `ANTHROPIC_API_KEY` ทุกคำตอบเลยได้ฉากสำรอง (`verdict: partial`) เหมือนกันหมด
แปลว่า acceptance ข้อ 4–7 ส่วนที่เป็น **verdict จริงจาก AI** ยังไม่เคยเดินผ่านของจริงเลย

- [ ] ใส่คีย์ใน `.env` แล้วรันสคริปต์เดโมใน README ให้ครบ
- [ ] ยืนยันว่า `กระโดดออกจากเรือแล้วเรือถอยหลัง` ได้ `correct` และได้ actor `person_a` + `boat`
- [ ] ยืนยันว่า `ผลักกำแพงแล้วกำแพงไม่ขยับ` ได้ `misconception` และถูกดันขึ้นจออัตโนมัติ
- [ ] ยืนยันว่า `ไม่รู้` ได้ `off_topic` และไม่มีการแต่งฉากขึ้นมา
- [ ] ยืนยันว่า distractor ที่ AI สร้าง (§6.4) ใช้ได้จริง ไม่ใช่แค่ค่าสำรองใน `GENERIC_DISTRACTORS`
- [ ] วัดเวลาจริงตาม §7.5 — พรีวิวขึ้นภายใน 1.5 วินาที ไฟล์เสร็จภายใน 4 วินาที

---

## 3. ทดสอบแบบห้องเรียนจริง

- [ ] ทดสอบด้วยเครื่องแยกกัน 3 เครื่อง (acceptance ข้อ 2)
      ที่ผ่านมาทดสอบด้วยสองแท็บในเบราว์เซอร์เดียว ซึ่ง **ใช้ localStorage ก้อนเดียวกัน** เลยกลายเป็นผู้เล่นคนเดียวกัน
- [ ] เปิดจอฉายบนจอโปรเจกเตอร์จริง อ่านจากหลังห้องออกไหม
- [ ] เปิดหน้านักเรียนบนมือถือจริง ปุ่มใหญ่พอไหม คีย์บอร์ดบังช่องพิมพ์ไหม
- [ ] ลองปิด wifi กลางคัน แล้วเปิดใหม่ ดูว่ากลับเข้าห้องได้เป็นคนเดิม

---

## 4. ช่องโหว่ที่รู้อยู่ แต่ยังไม่กระทบการใช้งาน

- **ตัวเลือกคำตอบสลับใหม่หลังเซิร์ฟเวอร์รีสตาร์ท** — `rt.choices` อยู่ในหน่วยความจำ ถ้ารีสตาร์ทกลางรอบ ลำดับตัวเลือกจะเปลี่ยน
- **นาฬิกาจับเวลาเป็นแค่ตัวเลขโชว์** — หมดเวลาแล้วไม่มีอะไรเกิดขึ้น ครูต้องกดเอง (สเปก §5 เขียนว่า "timer control" ซึ่งยังไม่มี)
  ตรวจแล้วว่า `guess:submit` ไม่ได้เช็ค `guessOpenedAt + GUESS_DURATION_MS` เลย และ `guessPoints()` มีพื้น 40 คะแนน
  แปลว่าคนที่ทายหลังนาฬิกาขึ้น 0 ยังได้ 40 คะแนนเท่าคนที่ทายวินาทีที่ 30 พอดี
- **ตั้งเวลาทายไม่ได้** — `GUESS_DURATION_MS` ฝังเป็นค่าคงที่ 30 วินาที
- **`?key=` อยู่ใน URL** — ติดใน history ของเบราว์เซอร์ พอสำหรับห้องเรียน แต่ไม่ควรใช้ที่อื่น
- **ถ้า SceneSpec ที่เก็บไว้ parse ไม่ผ่าน มีมจะหายเงียบ ๆ** จากรอบทาย โดยไม่มี log บอกครู

---

## 5. เพิ่มความสมบูรณ์

- [x] เทสต์ `canTransition()` ของ phase machine — `tests/phase.test.ts` เขียนตารางเส้นทางที่คาดหวังด้วยมือ ไม่ได้อ่านจาก `PHASE_TRANSITIONS` (ไม่งั้นเทสต์จะผ่านไม่ว่าตารางจะเขียนอะไร)
- [x] เทสต์ว่า socket ของนักเรียนสั่ง `teacher:phase` แล้วโดนปฏิเสธจริง — `tests/socket-auth.test.ts` รัน socket.io จริงบนพอร์ตชั่วคราว กับสำเนา `dev.db` ในโฟลเดอร์ temp
- [ ] สลับภาษาไทย/อังกฤษ — `src/lib/i18n/en.ts` เขียนครบแล้วแต่ยังไม่มีปุ่มเปลี่ยน
- [ ] เพิ่มสไปรต์ — ตอนนี้มี 8 ตัว คำตอบที่มีตัวละครแปลก ๆ เลยได้คนธรรมดาหมด
- [ ] ให้ครูจัดลำดับมีมบนจอเองได้ (ตอนนี้เรียงตามเวลาที่ส่งอย่างเดียว)
- [ ] หน้าสรุปหลังคาบ — คำตอบทั้งหมด + verdict + ประเด็นสอน ให้ครูเอาไปทบทวนต่อ

---

## 6. เรื่อง deploy (ถ้าจะเอาไปใช้จริงนอกเครื่องตัวเอง)

- ย้ายจาก SQLite ไป Postgres (schema รองรับอยู่แล้ว เปลี่ยน provider อย่างเดียว)
- ไฟล์มีมตอนนี้เขียนลงดิสก์เครื่องเซิร์ฟเวอร์ ถ้า deploy แบบหลาย instance ต้องย้ายไป object storage
- `runtimes` map อยู่ในหน่วยความจำของ process เดียว ถ้าสเกลเป็นหลาย instance ต้องมี Redis หรือบังคับ sticky session
